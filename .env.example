# Required for live baseline or response-supervisor runs. This template is safe to commit; `.env` is ignored.
OPENAI_API_KEY=

# Optional local runtime defaults used by climate-cascade-local, climate-cascade-api, and climate-cascade-worker.
CLIMATE_CASCADE_DATABASE_URL=sqlite:///var/climate-cascade.db
CLIMATE_CASCADE_ARTIFACT_ROOT=var/artifacts
